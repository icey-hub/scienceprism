# 需求清单（用户原始需求 + 追加要求）

> 本文件是**需求的单一事实源**。每次迭代前必读（见 `AGENTS.md`）。
> 需求只由用户新增或修改；agent 不得自行增删。变更时在文末追加，不覆盖历史。
> 下表保留原始需求文字；「现状」按 2026-09-25 的代码重新核对。早期迭代编号和 goal ID 是历史记录。

## 一、原始需求（首次提出）

| ID | 需求 | 现状 |
| --- | --- | --- |
| R-01 | 项目 agent 工作流还不完善，要完善 | 已实现 Harness、角色和项目工作流；整体产品质量仍在迭代。 |
| R-02 | 很多 skill 没添加，要补齐 | 已加入 9 个产品 Skill，并有绑定可达性测试；继续新增应按具体研究任务决定。 |
| R-03 | 有些约束是之前 AI 主观加的，要能识别与处置 | 已有约束注册表、溯源和审计记录；历史可疑项见 `constraint-audit.md`。 |
| R-04 | **新功能**：和 AI 对话后，AI 强制帮你加约束 | **未实现**对话提案、代码与测试草案、人工确认和生效的完整流程。 |
| R-05 | 约束**以代码形式**加，而不是加在 agent 本身（prompt） | 现有 17 条约束已进入代码注册表与测试；R-04 所需的新增约束生成器尚未实现。 |
| R-06 | 约束**可选择加不加** | 项目级策略文件已支持关闭 `standard` 约束、拒绝关闭 `core`；当前设置页只展示生效值，没有逐条 UI 开关。 |
| R-07 | agent 的角色设定不明确，要明确 | 8 个角色已定义并接入 Harness Runtime。 |
| R-08 | 要设置一个 goal | 曾建立 `goal-1475d1ce`；这是历史会话 ID，不代表当前仍有活动 goal。 |
| R-09 | 第一次提交先提交一个新分支 | ✅ `feat/agent-governance-r1` |
| R-10 | 迭代 10 次后，又开一个新分支 | ✅ 已完成（Round 1 = `feat/agent-governance-r1`，Round 2 = `feat/agent-governance-r2`，Round 3 = `feat/agent-governance-r3`） |
| R-11 | 每 10 次迭代要留一个文档给用户对比 | Round 1–6 的对比文档已保存于 `rounds/`；Round 7 尚未结束。 |
| R-12 | 文档绘图能力不行，要调研 skill 或 MCP 方案画好图 | ✅ **已完成**（迭代 021）：结论 = **手写 SVG**；零安装、离线、中文直出、可 diff、不越界写缓存。产物 `docs/agent-governance/assets/diagrams/`（4 张，含细胞结构图），方案见 `drawing-comparison.md` |
| R-13 | 画图方案也要对比 | ✅ **已完成**（迭代 021）：候选对比表含「是否经实际渲染验证」列，未验证项单独列明；细胞结构图为基准。MCP 路线经取证关闭（配置须写工作区外） |
| R-14 | 先列详细计划，先不执行 | ✅ 已完成（`plan.md`） |
| R-15 | **科研工具产出的文档要写入 `aidoc/` 目录**（不是 agent 治理文档） | ✅ **已完成**（迭代 037）：落点策略进了产品代码（`documentLanding.js`），驱动脚本**钉住** `aidoc/` 而不再依赖 gitignored 的 `.env`；产出路径越界即报错。注册为约束 **C-17** |

## 二、追加要求（执行过程中提出）

| ID | 追加要求 | 状态 |
| --- | --- | --- |
| U-01 | 必要时可多开子 agent，**最多 3 个**（总共 4 并发） | 已记录，见 `subagents/` |
| U-02 | **只允许在本工作区内下载/安装/修改**；工作区外的读写删除都不行 | 已写入 `AGENTS.md` 硬约束 |
| U-03 | skill 可以读取；工作区外文件**可以读**，但**不能改、不能删** | 已写入 `AGENTS.md` |
| U-04 | 按推荐值执行（D-1…D-6 全部采纳） | 历史决策与执行见 `iterations.md` 和各轮对比文档。 |
| U-05 | 执行本 goal 要**开三个子 agent** | 见 `subagents/` 三份任务书 |
| U-06 | 写 `AGENTS.md` 和迭代 README，**方便目标不偏移** | ✅ 已建（`AGENTS.md`、`README.md`） |
| U-07 | **绘图问题是**：只有简单线段，没有复杂矢量图；还有一些 emoji；复杂图（**例如细胞结构图**）画不出来 | ⚠️ 修正了绘图目标与 I-13 基准，见 `plan.md` 第 3.4 节 |
| U-08 | 需求、计划、三个子 agent 的要求也都要写成 md | ✅ 本文件 + `plan.md` + `subagents/*.md` |
| U-09 | **每次迭代前都要读取**这些 md | ✅ 已写入 `AGENTS.md` 与 `README.md` 的迭代前检查清单 |
| U-10 | **科研工具产出的文档**要写入 `aidoc/` 目录（不是 agent 自己的治理文档） | 已实现产物落点规则并有真实产物；agent 治理文档仍在 `docs/agent-governance/`。 |
| U-11 | **并发最多 3 个**；实测同时开 3 个子 agent **全部失败** | ✅ 已改为串行派发；实测 6 次派发 5 次失败，故以 Lead 串行为主 |
| U-12 | **用户未明确指示执行计划时，agent 不得擅自执行**；状态须记为「尚未进入执行计划 / 未修改代码」 | 当时的等待已由 U-18 解除；后续仍按用户当前授权范围执行。 |
| U-13 | **每次派子 agent 前，先查当前存活的子 agent 数；若已有 2 个存活，就不再派** | ⛔ 生效中，作为派发前的强制检查步骤 |
| U-14 | **整个 goal 暂停** | 历史暂停要求，后来由 U-18 的「开始执行计划」解除。 |
| U-15 | 子 agent 产出**删掉重来**；后续**以 Lead 为主**，子 agent 仅在必要时试 | ✅ 已删除 `agentRoles/` 与 `roleResolver.js`；实测 6 次派发 5 次失败，故改为 Lead 串行 |
| U-16 | **派子 agent 前若存活数 ≥ 2，必须先等已有子 agent 结束再派** | ⛔ 生效中，已写入 `AGENTS.md`「子 agent 派发规则」与 `README.md` 的迭代前检查清单 |
| U-17 | 澄清目录语义：`aidoc/` = **科研工具产出的文档**；`docs/agent-governance/` = agent 治理文档 | 已落实；两个目录均存在，`aidoc/` 已有真实产物。 |
| U-18 | **开始执行计划**；本轮迭代预算 **200 轮** | ✅ 已授权执行（U-12 的等待解除） |
| U-19 | 迭代按 **加 / 减 / 验证** 三拍循环：一轮加东西、一轮减东西、一轮验证 | ⛔ 生效中。**减**是硬要求：代码既要能加也要能删，不许只增不减 |
| U-20 | **本机 LLM 网关（`127.0.0.1:7864`）与 DSH 会话共享并发** | ⛔ 生效中：所有模型调用必须**串行、单发**，不得并发压测或批量并行；跑真实模型的迭代要控制调用次数 |
| U-21 | 迭代留下的文档必须是**项目真实跑出来的文档**，不是 agent 手写的 md | ⛔ 生效中：工具产出放 `aidoc/`；agent 的治理 md 只作内部过程记录，不作为交付物 |

## 三、由需求推导出的验收标准

| 需求 | 可执行验收 |
| --- | --- |
| R-04 / R-05 | 现有约束有代码与测试；R-04 还需验证「对话提案 → 生成代码和测试 → 人工确认 → 实际生效」全链路。 |
| R-06 | 关闭某条 `standard` 约束后它确实不再生效（负向测试），且审计留痕；`core` 级拒绝关闭 |
| R-07 | 角色注册表覆盖全部 AI 入口；越权角色被拒（负向测试） |
| R-11 | 每轮对比文档含上轮快照、10 次迭代清单、前后指标表、可视化图、可复现命令 |
| R-13 / U-07 | 同一参考图集（含**细胞结构图**）在候选方案下渲染，产物并排提交并打分 |
| U-02 | 无任何安装或缓存写入仓库外路径（边界自检） |

## 四、需求变更记录

- 2026-09-24：初版，汇总 R-01…R-14 与 U-01…U-09。
- 2026-09-24：追加 R-15 与 U-10…U-17。其中 U-10/U-17 纠正了目录语义：`aidoc/` 是**科研工具产出文档**的落地目录，agent 治理文档留在 `docs/agent-governance/`（上一版误把治理文档放进 `aidoc/`，已改回）。
- 2026-09-25：只校对状态与验收说明，不修改用户原始需求；明确 R-04 尚未实现，消除旧的「待执行」与「已完成」互相矛盾的状态。
