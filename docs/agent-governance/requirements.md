# 需求清单（用户原始需求 + 追加要求）

> 本文件是**需求的单一事实源**。每次迭代前必读（见 `AGENTS.md`）。
> 需求只由用户新增或修改；agent 不得自行增删。变更时在文末追加，不覆盖历史。

## 一、原始需求（首次提出）

| ID | 需求 | 现状 |
| --- | --- | --- |
| R-01 | 项目 agent 工作流还不完善，要完善 | 进行中（本 goal 主线） |
| R-02 | 很多 skill 没添加，要补齐 | 待做（I-09 产品侧 / I-10 开发侧） |
| R-03 | 有些约束是之前 AI 主观加的，要能识别与处置 | 取证完成：可疑项为 C-16 与 C-11 字段集；落地见 I-03 |
| R-04 | **新功能**：和 AI 对话后，AI 强制帮你加约束 | 待做（I-15 / I-16 / I-17 / I-18） |
| R-05 | 约束**以代码形式**加，而不是加在 agent 本身（prompt） | 待做（I-04 / I-05 / I-06） |
| R-06 | 约束**可选择加不加** | 待做（I-05：tier 分级 + 策略文件） |
| R-07 | agent 的角色设定不明确，要明确 | 待做（I-07 / I-08） |
| R-08 | 要设置一个 goal | ✅ 已建 `goal-1475d1ce` |
| R-09 | 第一次提交先提交一个新分支 | ✅ `feat/agent-governance-r1` |
| R-10 | 迭代 10 次后，又开一个新分支 | 待做（Round 2 = `feat/agent-governance-r2`） |
| R-11 | 每 10 次迭代要留一个文档给用户对比 | 待做（`rounds/round-01-comparison.md`、`round-02-comparison.md`） |
| R-12 | 文档绘图能力不行，要调研 skill 或 MCP 方案画好图 | 待做（I-11 / I-13 / I-14） |
| R-13 | 画图方案也要对比 | 待做（I-13，9 维打分 + 产物并排） |
| R-14 | 先列详细计划，先不执行 | ✅ 已完成（`plan.md`） |

## 二、追加要求（执行过程中提出）

| ID | 追加要求 | 状态 |
| --- | --- | --- |
| U-01 | 必要时可多开子 agent，**最多 3 个**（总共 4 并发） | 已记录，见 `subagents/` |
| U-02 | **只允许在本工作区内下载/安装/修改**；工作区外的读写删除都不行 | 已写入 `AGENTS.md` 硬约束 |
| U-03 | skill 可以读取；工作区外文件**可以读**，但**不能改、不能删** | 已写入 `AGENTS.md` |
| U-04 | 按推荐值执行（D-1…D-6 全部采纳） | ✅ 见 `README.md` 决策记录 |
| U-05 | 执行本 goal 要**开三个子 agent** | 见 `subagents/` 三份任务书 |
| U-06 | 写 `AGENTS.md` 和迭代 README，**方便目标不偏移** | ✅ 已建（`AGENTS.md`、`README.md`） |
| U-07 | **绘图问题是**：只有简单线段，没有复杂矢量图；还有一些 emoji；复杂图（**例如细胞结构图**）画不出来 | ⚠️ 修正了绘图目标与 I-13 基准，见 `plan.md` 第 3.4 节 |
| U-08 | 需求、计划、三个子 agent 的要求也都要写成 md | ✅ 本文件 + `plan.md` + `subagents/*.md` |
| U-09 | **每次迭代前都要读取**这些 md | ✅ 已写入 `AGENTS.md` 与 `README.md` 的迭代前检查清单 |
| U-10 | 产出文档必须保存到**一个 doc 文件夹**里 | ✅ 统一放 `docs/` 下（当前为 `docs/agent-governance/`）。**解释说明**：仓库既有约定就是 `docs/`，故未另建平行的 `doc/`；若要求字面目录名 `doc`，需用户确认后迁移 |
| U-11 | **并发最多 3 个**；实测同时开 3 个子 agent **全部失败** | ✅ 已改为**串行派发，一次最多 2 个**子 agent，并逐个确认返回后再派下一个 |
| U-12 | **用户未明确指示执行计划时，agent 不得擅自执行**；状态须记为「尚未进入执行计划 / 未修改代码」 | ⛔ 生效中：截至本条为止**未修改任何产品代码**（`apps/`、`packages/` 零改动）。后续每次改代码前必须先取得用户明确指示 |
| U-13 | **每次派子 agent 前，先查当前存活的子 agent 数；若已有 2 个存活，就不再派** | ⛔ 生效中，作为派发前的强制检查步骤 |

## 三、由需求推导出的验收标准

| 需求 | 可执行验收 |
| --- | --- |
| R-04 / R-05 | 约束以代码模块 + 测试形式落地；删除某条约束的测试会让门禁变红 |
| R-06 | 关闭某条 `standard` 约束后它确实不再生效（负向测试），且审计留痕；`core` 级拒绝关闭 |
| R-07 | 角色注册表覆盖全部 AI 入口；越权角色被拒（负向测试） |
| R-11 | 每轮对比文档含上轮快照、10 次迭代清单、前后指标表、可视化图、可复现命令 |
| R-13 / U-07 | 同一参考图集（含**细胞结构图**）在候选方案下渲染，产物并排提交并打分 |
| U-02 | 无任何安装或缓存写入仓库外路径（边界自检） |

## 四、需求变更记录

- 2026-09-24：初版，汇总 R-01…R-14 与 U-01…U-09。
- 待追加。
