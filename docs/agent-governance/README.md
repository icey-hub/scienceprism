# Agent 治理与迭代状态

> 这是当前状态入口。早期方案和每拍过程保留在 [需求记录](requirements.md)、[原执行计划](plan.md)、[迭代日志](iterations.md) 与 [轮次对比](rounds/) 中；那些文件的早期状态和测试数字是历史快照。

## 当前状态

截至 2026-09-25，迭代 065 的代码已合入并推送到 `main`；迭代 066 已校对使用文档。`npm run quality` 通过：126 项后端测试、前端类型检查、构建和 6 张图的布局检查。个人项目默认放在已忽略的 `data/`，科研示例产物放在 `aidoc/`。

| 目标 | 当前实现 |
| --- | --- |
| 项目约束 | 17 条约束有代码注册表、测试引用和项目级策略文件；项目设置页目前展示生效策略，不提供逐条开关操作。 |
| Agent 角色 | 8 个角色定义已接入 Harness；角色和能力可在运行前后查看。 |
| 研究 Skill | 9 个产品 Skill 位于 `.dsh/skills/`；只有会运行 Harness 的阶段绑定它们。 |
| 绘图与文档 | 已提交矢量图、结果图和研究示例文档；图表有可复现与布局检查。 |
| 对话后生成约束代码 | **尚未实现**。现有注册表和策略文件不等于自动生成、审批和启用新约束。 |
| 自洽性实验 | 只有未完成的本地检查点；没有正式发布结果。 |

上一轮本地质量整合修复了项目文件路径边界和项目概览提示，并补齐 CoT 实验的一条 HTTP 502 记录。实验仍有 600 条完整记录；补测后的答案错误，所以准确率不变，解析失败数从 2 调整为 1。详见 [迭代 065](iterations.md)。

## 本轮文档校对

迭代 066 对照当前入口、启动代码和设置持久化方式重写了 [中文 README](../../README_ZH.md) 与 [English README](../../README.md)，修正 [研究流程](../research-workflow.md) 的运行时说明，并把本文件的旧计划状态从当前状态中移出。文档只陈述已验证的行为；尚未实现的能力仍列在上表。

## 开发约定

改动前按 [AGENTS.md](../../AGENTS.md) 读取需求、计划和验证规则。每个迭代在仓库内完成修改、验证、更新本状态与 [迭代日志](iterations.md)，再提交。完整质量命令是 `npm run quality`（测试、类型检查、前端构建、图表布局检查）。

历史文件：

- [Round 1–6 对比文档](rounds/)：各轮当时的基线、变化和证据。
- [约束审计](constraint-audit.md) 与 [角色记录](agent-roles.md)：设计和实现依据。
- [验证纪律](playbooks/verification-discipline.md)：门禁破坏实验与工作区边界检查。

下一步需要单独设计并验证「对话提出约束 → 代码与测试草案 → 人工审阅 → 生效」的完整流程；目前不能把这项需求写成已完成。
